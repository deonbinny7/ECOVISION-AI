# _archive — Development / Debug Scripts

These files were used **during development only** and are **not required** for the running project.

| File | What it was used for |
|------|----------------------|
| `debug_gen.py` | Quick debugging of Grad-CAM generation |
| `gen_images.py` | Helper to generate sample test images |
| `train_backup.py` | Earlier version of training script before 2-phase fine-tuning was added |
| `verify_model.py` | One-off script to verify model output shapes after loading |

> Safe to ignore — no production code depends on these files.
